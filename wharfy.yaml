# wharfy.yaml — 最小宣言（未指定値は wharfy が推測する）
# channels を明示すると既定列を上書きするため、既存 owned を全て列挙したうえで
# apt / rpm / winget を追加する。
channels:
  - homebrew
  - scoop
  - releases
  - script
  - goinstall
  - apt
  - rpm
  - winget
